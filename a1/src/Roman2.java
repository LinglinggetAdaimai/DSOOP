package Roman;
public class Roman2 {
    
// check if curr is less than next,  
    // if yes then, check the rules, subtract the next and then plus it to the res, if not then break return 0

    // else if equal, check the next number if still equal then sum it up, but at one point that it's 4 times in a row break it

    // else if more than then just + normally to the result

}
